<!-- I will plan to have sth like this -->
<!-- List of alumni -->
<!-- list of users in a table  -->
<!-- goes down -->
<!-- goes down -->
<!-- Here is the Map -->


<!-- inside of the table, there will be categories like Name, Location, -->
<!-- Title (the job position), LinkedIn, Facebook, and then -->
<div class="page-header">
  <h3 ALIGN="LEFT">  </h3>  <!--  leave out an empty space ! -->
  <h3 ALIGN="LEFT" style="background-color:lightyellow; color:blue"> Senior Map for the Cal Class of 2016! </h3>
  <h1 ALIGN="CENTER" style="color:blue; background-color:lightyellow;"> List of Alumni </h1>
</div>


<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
            <th>Name</th>
            <th>City</th>
            <th>Title</th>
            <th>Linkedin</th>
            <th>Facebook</th>
            <th>Email</th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @users) do |user| %>
            <td><%= user.name %></td>
            <td><%= user.city %></td>
            <td><%= user.title %></td>
            <td><%= user.linkedIn %></td>
            <td><%= user.facebook %></td>
            <td><%= user.email%></td>
      <% end %>
    </tbody>
  </table>
</div>
